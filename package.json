{
  "name": "e3draft",
  "version": "1.0.0",
  "private": true,
  "description": "Fantasy football draft command center. Reads the shared Google Sheet board live, does the taken/available/tier math locally.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "refresh": "node tools/ingest/refresh.mjs",
    "recon": "node tools/ingest/recon.mjs",
    "set-login": "node tools/ingest/set-login.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
